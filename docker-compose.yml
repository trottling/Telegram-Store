x-logging: &default-logging
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"

# PUBLIC_PORT дописывается всегда (дефолт в .env.example — 443, обычный
# случай); :443 в URL безопасен и для CORS — admin_backend/middleware.CORS
# сравнивает origin'ы с нормализацией порта по умолчанию для схемы, а не
# байт-в-байт, так что "https://domain:443" и "https://domain" от браузера
# считаются одним и тем же origin'ом. Менять PUBLIC_PORT нужно, только если
# 443 на сервере занят чем-то посторонним — тогда
# весь публичный трафик магазина переезжает на новый порт одной строкой в .env.
x-public-urls: &public-urls
  ADMIN_PANEL_FRONTEND_URL: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}
  ADMIN_PANEL_CORS_ORIGIN: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}
  PAYMENTS_BACKEND_URL: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}/pay

# Явные публичные DNS вместо того, что Docker унаследует
x-public-dns: &public-dns
  dns:
    - 1.1.1.1
    - 8.8.8.8

services:
  # Одноразовый прогон миграций + бутстрап root-admin, затем выход.
  migrate:
    # image+build вместе: `docker compose pull && up -d` (без --build) просто
    # тянет готовый образ из GHCR (см. .github/workflows/build-images.yml) —
    # для слабого VPS сборка Go на месте не нужна. `docker compose build`
    # по-прежнему работает и соберёт локально под тем же тегом, если нужно.
    image: ghcr.io/trottling/telegram-store-migrate:latest
    build:
      context: .
      dockerfile: cmd/Dockerfile
      target: migrate
    container_name: telegram-store-migrate
    env_file:
      - path: .env
        required: true
    environment:
      <<: *public-urls
    depends_on:
      db:
        condition: service_healthy
    restart: "no"
    networks:
      - backend-network
    deploy:
      resources:
        limits:
          memory: 160M

  bot:
    image: ghcr.io/trottling/telegram-store-bot:latest
    build:
      context: .
      dockerfile: cmd/Dockerfile
      target: bot
    container_name: telegram-store-bot
    env_file:
      - path: .env
        required: true
    environment:
      <<: *public-urls
      GOMAXPROCS: 1
      GOMEMLIMIT: 130MiB
      BOT_WEBHOOK_URL: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}/bot
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - public-network
      - backend-network
    deploy:
      resources:
        limits:
          memory: 160M
    <<: [ *default-logging, *public-dns ]

  admin-backend:
    image: ghcr.io/trottling/telegram-store-admin-backend:latest
    build:
      context: .
      dockerfile: cmd/Dockerfile
      target: admin_backend
    container_name: telegram-store-admin-backend
    env_file:
      - path: .env
        required: true
    environment:
      <<: *public-urls
      GOMAXPROCS: 1
      GOMEMLIMIT: 130MiB
    # порт наружу не пробрасываем — снаружи торчит только caddy, до admin-backend
    # он достаёт по имени сервиса внутри public-network
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - public-network
      - backend-network
    deploy:
      resources:
        limits:
          memory: 160M
    <<: *default-logging

  payments-backend:
    image: ghcr.io/trottling/telegram-store-payments-backend:latest
    build:
      context: .
      dockerfile: cmd/Dockerfile
      target: payments_backend
    container_name: telegram-store-payments-backend
    env_file:
      - path: .env
        required: true
    environment:
      <<: *public-urls
      GOMAXPROCS: 1
      GOMEMLIMIT: 100MiB
    # порт наружу не пробрасываем — вебхуки мерчантов идут через caddy (путь /pay на $DOMAIN_NAME)
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped
    networks:
      - public-network
      - backend-network
    deploy:
      resources:
        limits:
          memory: 128M
    <<: [ *default-logging, *public-dns ]

  # Не долгоживущий сервис: копирует собранную статику панели в volume, который
  # читает основной caddy (см. Caddyfile, handle "/"), и завершается —
  # Exited(0) в docker ps тут норма, тот же паттерн, что и у migrate. Раньше
  # это был отдельный nginx-контейнер за reverse_proxy; своего порта/сервера
  # больше нет.
  admin-frontend:
    # Образ из GHCR собран с пустым VITE_API_BASE_URL (относительный /api/...,
    # один и тот же домен что у панели, что у API — см. workflow) и годится
    # для любого DOMAIN_NAME без пересборки. build.args ниже — только для
    # локального `docker compose build`.
    image: ghcr.io/trottling/telegram-store-admin-frontend:latest
    build:
      context: ./admin_frontend
      dockerfile: Dockerfile
      target: dist-export
      args:
        VITE_API_BASE_URL: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}
    container_name: telegram-store-admin-frontend
    restart: "no"
    volumes:
      - admin_frontend_dist:/output
    deploy:
      resources:
        limits:
          memory: 64M
    <<: *default-logging

  # TLS-терминатор: единственный сервис с портами наружу (80/443), автовыпуск
  # сертификатов Let's Encrypt по ACME HTTP-01/TLS-ALPN. Домен и почта — из
  # DOMAIN_NAME/ACME_EMAIL (см. .env); один хост на всё, панель/API/вебхуки
  # платежей/вебхуки Telegram/Grafana разведены по пути (/, /api, /pay, /bot,
  # /stats) прямо в Caddyfile.
  caddy:
    image: caddy:2-alpine
    container_name: telegram-store-caddy
    env_file:
      - path: .env
        required: true
    ports:
      - "80:80"
      - "${PUBLIC_PORT:-443}:${PUBLIC_PORT:-443}"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - admin_frontend_dist:/srv/admin-frontend:ro
      - caddy_data:/data
      - caddy_config:/config
    # admin-frontend — не долгоживущий сервис (копирует dist/ и выходит, см.
    # его комментарий выше), поэтому condition отличается: caddy должен
    # дождаться, что копирование реально завершилось, а не просто что
    # контейнер запустился, иначе первый старт отдал бы пустой /srv/admin-frontend.
    depends_on:
      bot:
        condition: service_started
      admin-backend:
        condition: service_started
      payments-backend:
        condition: service_started
      admin-frontend:
        condition: service_completed_successfully
      grafana:
        condition: service_started
    restart: unless-stopped
    networks:
      - public-network
    deploy:
      resources:
        limits:
          memory: 96M
    <<: [ *default-logging, *public-dns ]

  db:
    image: postgres:18-alpine
    container_name: telegram-store-db
    # -c log_min_messages=... — тот же приём, что и command у redis ниже:
    # переопределяем один GUC через аргумент запуска, не через смонтированный
    # postgresql.conf.
    command: [ "postgres", "-c", "log_min_messages=${POSTGRES_LOG_LEVEL:-warning}" ]
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_NAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - backend-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 448M
    <<: *default-logging

  redis:
    image: redis:8-alpine
    container_name: telegram-store-redis
    # ACL-пользователь из REDIS_USERNAME/REDIS_PASSWORD, default отключён —
    # подключиться можно только этой парой, requirepass одного пароля недостаточно
    # (config.New() отдельно проверяет, что обе переменные не пустые и не
    # плейсхолдер из .env.example).
    command: [
      # По умолчанию notice — и тогда в лог попадает каждое сохранение RDB/AOF
      # фонового процесса. warning (REDIS_LOG_LEVEL) оставляет то, что реально
      # стоит увидеть (у Redis нет уровня "warn", только полное "warning").
      "redis-server", "--appendonly", "yes", "--loglevel", "${REDIS_LOG_LEVEL:-warning}",
      "--user", "default", "off",
      "--user", "${REDIS_USERNAME}", "on", ">${REDIS_PASSWORD}", "allcommands", "allkeys", "allchannels"
    ]
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "--user", "${REDIS_USERNAME}", "-a", "${REDIS_PASSWORD}", "--no-auth-warning", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - backend-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
    <<: *default-logging

  # Ежедневный pg_dump + опциональная выгрузка в S3 (rclone), см. backup/.
  # На public-network только ради исходящего трафика к S3-эндпоинту —
  # backend-network (internal: true) наружу не роутит.
  backup:
    build:
      context: ./backup
      dockerfile: Dockerfile
    # crond из dcron сам себя переводит в отдельную группу процессов (setpgid)
    # при старте — ядро отказывает в этом PID 1 контейнера на части хостов
    # ("setpgid: Operation not permitted", вечный restart-loop). init: true
    # подставляет перед CMD настоящий init (tini) как PID 1, crond после этого
    # получает нормальный PID 2 и спокойно себя переводит в группу.
    init: true
    container_name: telegram-store-backup
    env_file:
      - path: .env
        required: true
    environment:
      RETENTION_DAYS: 7
    volumes:
      - backup_data:/backups
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - backend-network
      - public-network
    deploy:
      resources:
        limits:
          memory: 224M
    <<: [ *default-logging, *public-dns ]

  # Метрики бота (см. BOT_METRICS_PORT) — сам себя тоже скрейпит, см.
  # monitoring/prometheus/prometheus.yml. Наружу не торчит — доступ только
  # через Grafana (datasource) или изнутри docker-сети.
  prometheus:
    image: prom/prometheus:v3
    container_name: telegram-store-prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"
      # По умолчанию info — и тогда в лог попадает каждый scrape каждой цели
      # (bot/admin-backend/payments-backend/сам себя) раз в интервал. warn
      # (PROMETHEUS_LOG_LEVEL) оставляет то, что реально стоит увидеть.
      - "--log.level=${PROMETHEUS_LOG_LEVEL:-warn}"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    networks:
      - backend-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 192M
    <<: *default-logging

  # Хранилище логов. -config.expand-env=true — чтобы ${LOKI_RETENTION_DAYS} в
  # смонтированном конфиге подставился самим Loki, а не docker-compose (тот
  # ${VAR}-синтаксис на файлы внутри контейнеров не распространяется).
  loki:
    image: grafana/loki:3.7.6
    container_name: telegram-store-loki
    command: [ "-config.file=/etc/loki/local-config.yaml", "-config.expand-env=true" ]
    environment:
      LOKI_RETENTION_DAYS: 14
      LOKI_LOG_LEVEL: ${LOKI_LOG_LEVEL:-warn}
    volumes:
      - ./monitoring/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki_data:/loki
    networks:
      - backend-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 192M
    <<: *default-logging

  # Пересылает логи всех контейнеров в Loki — находит их сам через Docker
  # Engine API (тот же docker.sock, что раньше монтировал Promtail), ни один
  # существующий сервис трогать не нужно. В отличие от Promtail, читает через
  # API, а не с диска — /var/lib/docker/containers монтировать не нужно.
  # Сам себя из сбора логов исключает (self-container detection).
  vector:
    image: timberio/vector:0.57.0-alpine
    container_name: telegram-store-vector
    environment:
      # По умолчанию info — и тогда в лог попадает каждое обработанное
      # событие. warn (VECTOR_LOG_LEVEL) оставляет то, что реально стоит
      # увидеть — VECTOR_LOG это родное имя переменной у самого Vector.
      VECTOR_LOG: ${VECTOR_LOG_LEVEL:-warn}
    command: [ "--config-yaml", "/etc/vector/vector.yaml" ]
    volumes:
      - ./monitoring/vector/vector.yaml:/etc/vector/vector.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - loki
    networks:
      - backend-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 64M
    <<: *default-logging

  # Единственный публичный вход во весь observability-стек ($DOMAIN_NAME/stats
  # в Caddyfile, саб-путь — см. GF_SERVER_SERVE_FROM_SUB_PATH). Логин не свой —
  # auth.proxy доверяет X-WEBAUTH-USER, который проставляет admin-backend через
  # forward_auth в Caddy (тот же код из /admin, что и в веб-панель).
  # GRAFANA_ADMIN_USER/PASSWORD — только break-glass, доступны исключительно
  # изнутри docker-сети, публично эта форма входа не показывается.
  grafana:
    image: grafana/grafana:13.1
    container_name: telegram-store-grafana
    env_file:
      - path: .env
        required: true
    environment:
      # По умолчанию info — и тогда в лог попадает вообще любой рутинный запрос
      # (datasource healthcheck, session cleanup и т.п.) на уровне HTTP-сервера
      # Grafana. warn (GRAFANA_LOG_LEVEL) оставляет то, что реально стоит увидеть.
      GF_LOG_LEVEL: ${GRAFANA_LOG_LEVEL:-warn}
      GF_AUTH_PROXY_ENABLED: "true"
      GF_AUTH_PROXY_HEADER_NAME: X-WEBAUTH-USER
      GF_AUTH_PROXY_HEADER_PROPERTY: username
      GF_AUTH_PROXY_AUTO_SIGN_UP: "true"
      GF_USERS_AUTO_ASSIGN_ORG_ROLE: Editor
      GF_USERS_ALLOW_SIGN_UP: "false"
      # Grafana сама объявляет свои роуты под /stats/ и ожидает, что Caddy
      # проксирует путь как есть, не срезая префикс — см. Caddyfile.
      GF_SERVER_ROOT_URL: https://${DOMAIN_NAME}:${PUBLIC_PORT:-443}/stats/
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
      - loki
    restart: unless-stopped
    # оба network: backend — достать prometheus/loki, public — чтобы caddy
    # нашёл её по имени (тот же паттерн, что у admin-backend).
    networks:
      - backend-network
      - public-network
    deploy:
      resources:
        limits:
          memory: 256M
    <<: *default-logging

networks:
  # db+redis видны только bot/admin-backend/payments-backend/migrate/backup/
  # prometheus/loki/vector/grafana, без выхода наружу
  backend-network:
    driver: bridge
    internal: true
  # нужна для исходящих к Telegram (bot), к S3 (backup) и для caddy/admin-backend/
  # payments-backend/admin-frontend/grafana, между которыми ходят проксируемые запросы.
  # Подсеть закреплена, потому что admin-backend доверяет X-Forwarded-For только
  # от неё (ADMIN_PANEL_TRUSTED_PROXIES) — на плавающем адресе из дефолтного
  # пула Docker это доверие пришлось бы выдавать вслепую.
  public-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

volumes:
  postgres_data:
  redis_data:
  admin_frontend_dist:
  caddy_data:
  caddy_config:
  backup_data:
  prometheus_data:
  loki_data:
  grafana_data:
